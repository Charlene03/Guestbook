<nav class="navbar navbar-inverse"> <!-- navbar-default表示這裡顯示的是預設值的navbar，底色是灰色的。-->
    <div class="container-fluid">
 		 <div class="navbar-header"> <!--放導覽列的標頭，後面放網頁的logo-->
 	      	<%= link_to "GuestBooks", root_path, :class => 'navbar-brand' %>
 		 </div>
		 <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
 	    	
 	    	<ul class="nav navbar-nav"> <!--nav navbar-nav二個都是class關鍵字-->
            <% if logged_in? %>
        		<li><%= link_to "Welcome! #{current_user.name}" %></li>
        		<li><%= link_to 'New Post', new_post_path %></li>
            <% else %>
                <li><%= link_to 'Register', register_path %></li>
            <% end %>
	    	</ul>
	    	
	    	<ul class="nav navbar-nav navbar-right">
            <% if logged_in? %>
                <li class="dropdown">
                  <%= link_to '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown' do %>
                  <%= current_user.name %> <span class='caret'></span>
                  <% end %>
                    <ul class="dropdown-menu">
                        <li><%= link_to 'User Profile', user_path(current_user) %></li>
                        <li role="separator" class="divider"></li>
                        <li><%= link_to 'Logout', logout_path %></li>
                    </ul>
                </li>
            <% else %>
                <li><%= link_to 'Login', login_path %></li>
            <% end %>
            </ul>
            
    	</div>   
 	</div>
</nav> 